파이썬 

**은 제곱(우선순위 곱셈보다 높음
// 정수의 나눗셈
%은 실수까지 가능

c 언어는 strict타입의 언어이지만
파이썬은 한 변수로 모든걸 가능

True False 대문자 사용

inclusive exclusive
포함      비포함
 

name[ start : stop : step ]
step = [::2] 두칸씩 띄면서 출력
[::-1] 역으로 출력


OOP특성 : List가 아닌거에는 append를 쓸수없지만 List이면 쓸수있다.
List.append

Slice string, list같은 collection타입에 다 쓸 수 있다.

Dictionary
키와 value의 조합을 저장

del 은 메모리에서 완전히 삭제하는것
del a
del score['momo']

키가 존재하는지 확인하는것 'zwi' in score
			True

Score.clear


Tuple 소괄호 사용


import turtle

from turtle import *
하면 turtle.forward가 아닌 forward사용가능

randrange[ ), randint [ ]


문자열도 == 가능

' ' , " " 분간 안함


튜플이라는것을 이용해서 쌍을 이용해서 대입해야하는 변수를 한꺼번에 대입한다.

a = 'AAA'
b = 'BBB'

a,b = b,a

a = 'BBB'
b = 'AAA'

튜플, 리스트 리턴 가능

event driven, message driven 
코드의 컨트롤을 사용자가 아니라 시스템에게 맡기는것

이 함수는 리턴을하지 않는다

이벤트 함수를 만들면 시스템이 이벤트를 기다리다가 이벤트가 발생하면
사용자에게 알려주는 시스템

이벤트 핸들러, 콜백

pibot 그리는 대상 (어느걸 기준으로 그릴것인가)

thumb.db 윈도우에서 썸네일 만드는 파일 ignore 할것

데이터와 데이터를 다루는 행위 incapsulation

특정 속성과 속성을 가진 애들을 묶어두는것

게임을 구성하는것을 game object라 칭하기로함

type 은 clas라 생각해도 된다. 속성이라 생각해도 된다.

붕어빵과 계란빵은 class가 다르다 붕어빵들은 비늘,팥 의 스키마는 같지만
팥의 양은 다를 수 있다.

oop는 실생활을 잘 묘사함

구조체배열 

boys = [ boy() ] * 30 는 안됨 하나의 객체를 30번 복사한것

boys = [ boy() for i in range(20) ]

random.uniform (1.0, 2.0) 1.0~2.0사이의 값

open_canvas()
뒤에 road_image를 해야 함

20명의 속도가 다른 애들이 마우스를 따라다님


모듈 파이썬의 정의와 문장을 담고있는 파일
임포트 하면 하나의 객체(싱글톤):하나의 클래스 하나의 객체
 state 조건과 종류 중간고사

state 의 exit()에서 메모리를 free안하면
다시 왔을때 사용가능한가?

scipy 임포트 과학계산관련 모듈
numpy 파이썬 라이브러리

json 자바스크립트에서 쓰는 텍스트 형식 표준
{} 객체 []array
딕셔너리와 json json은 문자열
딕셔너리의 for roof
json.Loads 스트링
json.Load 파일

funcs() 함수를 저장

clip_composite_draw 각도
clamp(1,x,300) 1과300사이의 값이 x

상태변화 -> 이벤트로부터
상태 다이어그램 -> 상태변화를 화살표로

json & jsp trailing comma
마지막에 콤마를 붙여도 되는가? json은 허락하지 않는다.
json은 {hello:"world"} 안된다(키값으로 문자열) jsp에서는 된다

중간고사: oop 용어들, 상속, 인캡슐레이션 오리너구리, colimofizon?
concept type class
variable
파이썬
dictionary, array, json
drawnow, draw 왜 두개가있는지
if와 for의 위치
모듈임포트, 터틀그래픽스(빈칸채우기) 15문제안팎

atan2는 y부터

코드가 비효율적으로 사용되는것을 막음
frame time
game_world 제너레이터 yeild라는 키워드
충돌체크, 스크롤
json pickle 모듈들끼리의 상호작용
수학함수들

223호

