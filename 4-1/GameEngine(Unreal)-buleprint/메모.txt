Light baking 움직이지 않는 액터에게 그림자를 미리 그려놓는것

위치변환중 end키는 아래쪽에 고정
v키는 위쪽에 고정

https://openkakao.com/o/gNXZKzhb

스크립트언어 컴파일 없이 실행
인터프리터 방식
블루프린트의 이름들 = c++함수이름

블루프린트 종류가 있다.

Blueprint Class
새로운종류의 액터를 설계하고 생성
설계
외형, 로직(행동), 물리적인 특성

블루프린트 설계요령
중첩금지. 비슷하면(복제후 수정, 파생후 추가)
새로만들면 Actor를 상속(Base Class)

staticMash 는 기본적으로 움직임이 제한
Light build를 위해

컴포넌트는 트리구조로 담긴다.
하위 컴포넌트들도 영향을 받기 위해서

Root는 더미 node
zFighting 같은z좌표일때 두물체의 출력이 경쟁

블루프린트 비주얼 스크립트 언어

노드와 연결로 코딩을 한다.


event Beginplay

event Tick
-Delta Seconds 프레임과 프레임 사이의 시간

블루프린트는 실행 시 비주얼로 실행을 보여줌
break point 가능

ctrl키는 get
alt키는 set

변수를 노드(하얀 화살표)에 드래그하면 set
dynamic casting- 실시간으로는 불가능

gimi 플레이어의 행동을 도와주는
Flatform 플레이어의 움직임을 도와주는 판

alt+ 선 클릭 = 삭제


Actor Merging - static Mesh 오브젝트를 하나로 변환
마우스 휠클릭 - 피봇포인트 임시변환

Construction Script
Blueprint Class의 구성을 프로그래밍적으로 하기 위한 스크립트. 
C++의 Constructor(생성자)와 유사한 기능. -객체의 모양을 잡아주는 역할

액터의 속성에 제한을 넣을 수 있다.

엑터의 벡터변수는
엑터의 로컬원점을 기준으로 움직인다.

소규모의 많은 계산은 퍼포먼스를 낮춘다.
많은계산을 한번에 할 수 있는 함수를 찾는다.

두 벡터의 내적을 이용해서 각도를 구한다.

레벨 블루프린트
레벨상의 엑터들끼리의 상호작용
전역변수처럼 직접 제어가능
빠른 테스트에 씀. 키보드,마우스 직접적으로 처리