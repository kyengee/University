world {
    inventory {
        # Contains all world inventory groups.
        groups {
            # Example world inventory group.
            example="world, DIM1, DIM-1"
        }
        # Toggles separate inventories between worlds.
        separate=false
    }
    weather {
        # Worlds listed here, will not change weather.
        locked=null
    }
}
